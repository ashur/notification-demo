---
layout: layouts/base.njk

prefetch:
  - href: /images/bell.svg
    as: image
---
<main
	class="[][ relative ][]"
>
	{% include "components/Notification.njk" %}

	<article
		class="[ stack-lg ][][ article ]"
	>
		{% if image %}
		{% Image "src/images/" + image.src, image.alt %}
		{% endif %}

		<div class="[ inset stack-lg ][][]">
			<section class="[ stack ][][ article-contents ]">
				<h1>{{ title or site.title }}</h1>

				{{ content | safe }}
			</section>

			<aside class="[ center stack ][][ aside-reset ]">
				<p>A real site probably wouldn’t give users the option to monkey with the notification state, but this is a demo so</p>
				<button
					onclick="notification.reset()"
					class="[][][ demo-reset ]"
				>Mark as Unread</button>
			</aside>
		</div>
	</article>
</main>
